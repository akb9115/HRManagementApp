import { SortByPropertyPipe } from './sort-by-property.pipe';

describe('SortByPropertyPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByPropertyPipe();
    expect(pipe).toBeTruthy();
  });
});
